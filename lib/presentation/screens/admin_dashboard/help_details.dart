import 'package:flutter/material.dart';

class HelpDetails extends StatelessWidget {
  const HelpDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Help Details Page'));
  }
}
